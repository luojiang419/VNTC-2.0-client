# 002-自动清理TUN网卡功能前备份

## 备份时间

2026-07-08

## 功能阶段

设置页新增自动清理无用 TUN 虚拟网卡功能。

## 本阶段计划修改文件

- `vnt-2.0.0/vntc-manager/src/main.rs`
- `vntc2_flutter/lib/core/models.dart`
- `vntc2_flutter/lib/core/vnt_runtime.dart`
- `vntc2_flutter/lib/core/dashboard_controller.dart`
- `vntc2_flutter/lib/main.dart`

## 修改前状态摘要

- Rust Manager 目前已有 `/profiles/connect`、`/profiles/disconnect`、`/peers`、`/internal/shutdown` 等接口，没有虚拟网卡清理接口。
- `AppSettings` 目前包含语言、深色模式、关闭行为、开机自启、静默自启、启动连接默认配置、选中配置、默认配置等字段，没有 TUN 网卡清理开关。
- 设置弹窗目前展示语言、关闭动作、默认配置、开机自启、静默自启、启动后连接默认配置等选项，没有清理虚拟网卡相关入口。

## 修改前关键代码摘录

### Rust Manager 路由

```rust
let app = Router::new()
    .route("/health", get(health))
    .route("/dashboard/summary", get(get_dashboard_summary))
    .route("/profiles", get(get_profiles))
    .route("/profiles/connect", post(connect_profiles))
    .route("/profiles/disconnect", post(disconnect_profiles))
    .route("/peers", get(get_peers))
    .route("/internal/shutdown", post(shutdown_manager))
    .with_state(state.clone());
```

### AppSettings 字段

```dart
final AppLanguage language;
final bool darkMode;
final CloseAction closeAction;
final bool autoStart;
final bool silentAutoStart;
final bool connectDefaultOnLaunch;
final String? selectedProfileId;
final String? defaultProfileId;
```

### 设置页现有开关

```dart
SwitchListTile(
  contentPadding: EdgeInsets.zero,
  value: connectDefaultOnLaunch,
  title: Text(
    t(
      '启动后连接默认配置或勾选配置',
      'Connect default or selected profiles on launch',
    ),
  ),
  onChanged: (value) =>
      setState(() => connectDefaultOnLaunch = value),
),
```

## 待补充

已在本阶段完成后补充最终代码前后对比和验证结果。

## 最终修改后状态摘要

- Rust Manager 新增 `/adapters/cleanup-unused` 接口。
- Flutter 设置新增 `cleanupUnusedTunOnLaunch` 持久化字段。
- 应用启动时如果开关启用，会在自动连接配置前静默清理无用 `vntc-*` 网卡。
- 设置页新增“自动清理无用 TUN 虚拟网卡”开关，首次开启保存后立即执行一次清理并显示结果。
- 最终 Windows release 已输出到 `output/vntc2_flutter-windows-release`。

## 最终代码前后对比

### Rust Manager：新增清理接口

修改前：

```rust
.route("/profiles/connect", post(connect_profiles))
.route("/profiles/disconnect", post(disconnect_profiles))
.route("/peers", get(get_peers))
```

修改后：

```rust
.route("/profiles/connect", post(connect_profiles))
.route("/profiles/disconnect", post(disconnect_profiles))
.route(
    "/adapters/cleanup-unused",
    post(cleanup_unused_tun_adapters),
)
.route("/peers", get(get_peers))
```

新增清理实现：

```rust
fn cleanup_unused_tun_adapters(&self) -> anyhow::Result<TunAdapterCleanupResult> {
    self.cleanup_exited_sessions();
    #[cfg(windows)]
    {
        return self.cleanup_unused_tun_adapters_windows();
    }
    #[cfg(not(windows))]
    {
        Ok(TunAdapterCleanupResult {
            cleaned: Vec::new(),
            kept: Vec::new(),
            skipped: Vec::new(),
            unsupported: true,
        })
    }
}
```

### Flutter：设置模型新增字段

修改前：

```dart
final bool autoStart;
final bool silentAutoStart;
final bool connectDefaultOnLaunch;
```

修改后：

```dart
final bool autoStart;
final bool silentAutoStart;
final bool connectDefaultOnLaunch;
final bool cleanupUnusedTunOnLaunch;
```

### Flutter：启动自动清理

修改前：

```dart
await runtime!.start();
await refreshAll();
```

修改后：

```dart
await runtime!.start();
if (settings.cleanupUnusedTunOnLaunch) {
  await cleanupUnusedTunAdapters(silent: true);
}
await refreshAll();
```

### Flutter：设置页新增开关

新增：

```dart
SwitchListTile(
  contentPadding: EdgeInsets.zero,
  value: cleanupUnusedTunOnLaunch,
  title: Text(
    t(
      '自动清理无用 TUN 虚拟网卡',
      'Automatically clean unused TUN adapters',
    ),
  ),
  onChanged: (value) =>
      setState(() => cleanupUnusedTunOnLaunch = value),
),
```

## 验证结果

- `cargo fmt -p vntc_manager`：通过。
- `cargo check -p vntc_manager`：通过。
- `cargo build --release -p vnt2 --bin vnt2_cli --features vnt-ipc`：通过。
- `cargo build --release -p vnt2 --bin vnt2_ctrl --features vnt-ipc`：通过。
- `cargo build --release -p vntc_manager`：通过。
- `D:\flutter\bin\dart.bat format ...`：通过。
- `D:\flutter\bin\flutter.bat analyze`：通过，`No issues found!`
- `D:\flutter\bin\flutter.bat build windows --release`：第一次因中文路径编码失败；已通过临时 `V:` ASCII 盘符映射重试成功。
- 已复制最终产物到 `output/vntc2_flutter-windows-release`。
- 已执行 `flutter clean` 清理 Flutter 中间缓存，并移除临时 `V:` 盘符映射。
