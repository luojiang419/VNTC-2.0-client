# 开发进度快照

更新日期：2026-05-15

## 1. 仓库与同步状态

- 本地项目目录：`D:\app\VNT2X`
- GitHub 仓库地址：`https://github.com/luojiang419/VNTC-2.0-client-.git`
- 当前分支：`main`
- 当前目标：以后续开发为主，保持从当前基线继续迭代
- 本快照创建后，将与当前代码一起提交并推送到 GitHub

## 2. 当前已完成功能

### 客户端架构

- 已完成 `Flutter Windows + Rust Manager + vnt2_cli` 的桌面客户端架构
- 已完成 `Rust Manager` 对多个配置的统一调度
- 已支持多配置勾选后批量连接 / 批量断开

### 配置与连接

- 每个配置保存时自动分配唯一：
  - `tun_name`
  - `ctrl_port`
  - `device_id`
- 已支持配置持久化到 `data/profiles/<id>/`
- 已支持高级配置折叠编辑，并写入 `vnt.toml`
- 已移除“无 TUN 测试模式”开关，默认按正常连接模式使用

### 主界面

- 已完成单页主界面
- 已完成顶部汇总状态栏
- 已完成配置列表卡片
- 已完成在线 / 离线用户区域
- 已将在线 / 离线数量改为可点击标签页按钮
- 默认显示在线用户，点击离线标签页后切换显示离线用户
- 已支持点击用户虚拟 IP 后方复制图标，将虚拟 IP 复制到系统剪贴板
- 已在“编辑配置”左侧增加“添加配置”

### Windows 桌面行为

- 已接入托盘最小化能力
- 已支持关闭动作：
  - 直接关闭
  - 最小化到托盘
  - 每次询问
- 已支持托盘图标显示
- 已支持托盘右键快捷菜单
- 已支持单击托盘图标恢复窗口
- 当前默认窗口尺寸：`800x800`
- 当前最小窗口尺寸：`800x800`

### 发布与文档

- 已整理根目录 `README.md`
- 已将软件截图接入 GitHub 首页展示
- `软件首页.png` 已作为项目首页封面图
- 其他截图已用于详情展示
- 已可构建本地发布目录 `dist/`

## 3. 当前代码基线说明

当前仓库中的代码已经包含以下重要实现：

- 多配置并发连接能力
- 聚合在线 / 离线用户展示
- 高级配置面板
- 剪贴板复制虚拟 IP
- 托盘图标与托盘菜单
- 默认窗口尺寸 `800x800`

后续继续开发时，应以当前仓库根目录为唯一基线，不再回退到旧版单配置 / 无托盘 / 无高级配置实现。

## 4. 当前仍可继续迭代的事项

以下事项尚可在后续版本继续增强：

- 开机自启的系统级挂接
- 托盘菜单项的进一步细化
- 高级配置的分组化展示
- 打包安装器 / 发布说明完善
- UI 动效与视觉细节继续打磨

## 5. 后续继续开发建议

建议后续从以下顺序继续：

1. 先完善开机自启和静默启动的系统级实现
2. 再优化高级配置的分组和输入校验
3. 然后补安装包、发布说明和版本管理
4. 最后继续打磨 UI 细节和异常提示体验

## 6. 备注

- 本快照文件用于记录“本次开发已达到的状态”
- 后续继续开发时，优先以 GitHub 仓库 `main` 分支最新提交为准
- 若本地还有新改动，应继续在当前仓库基础上提交，不另起独立目录
