# 010-自动清理TUN网卡功能完成

## 已完成内容

- 完成设置页“自动清理无用 TUN 虚拟网卡”功能。
- Rust Manager 新增 `/adapters/cleanup-unused` 清理接口。
- Flutter 设置新增 `cleanupUnusedTunOnLaunch` 持久化字段。
- 应用启动时如果开关开启，会在自动连接配置前静默清理一次。
- 用户首次开启开关并保存后，会立即清理一次并显示结果提示。
- 清理范围限制在本客户端生成的 `vntc-*` 网卡，并保留当前配置仍引用的网卡；状态为 `Up` 的未引用网卡会跳过。
- 已更新 Windows release 产物到 `output/vntc2_flutter-windows-release`。
- 已清理 Flutter 构建中间缓存，并移除临时 `V:` 盘符映射。

## 当前修改到哪个模块

模块 4：全局验证、构建产物更新、缓存清理与备份文档收尾。

## 具体修改的代码前后对比

### Rust Manager 接口

修改前：

```rust
.route("/profiles/connect", post(connect_profiles))
.route("/profiles/disconnect", post(disconnect_profiles))
.route("/peers", get(get_peers))
```

修改后：

```rust
.route("/profiles/connect", post(connect_profiles))
.route("/profiles/disconnect", post(disconnect_profiles))
.route(
    "/adapters/cleanup-unused",
    post(cleanup_unused_tun_adapters),
)
.route("/peers", get(get_peers))
```

### Flutter 设置页

修改前：设置页没有 TUN 网卡清理选项。

修改后：

```dart
SwitchListTile(
  contentPadding: EdgeInsets.zero,
  value: cleanupUnusedTunOnLaunch,
  title: Text(
    t(
      '自动清理无用 TUN 虚拟网卡',
      'Automatically clean unused TUN adapters',
    ),
  ),
  onChanged: (value) =>
      setState(() => cleanupUnusedTunOnLaunch = value),
),
```

### 启动流程

修改前：

```dart
await runtime!.start();
await refreshAll();
```

修改后：

```dart
await runtime!.start();
if (settings.cleanupUnusedTunOnLaunch) {
  await cleanupUnusedTunAdapters(silent: true);
}
await refreshAll();
```

## 验证结果

- `cargo fmt -p vntc_manager`：通过。
- `cargo check -p vntc_manager`：通过。
- `cargo build --release -p vnt2 --bin vnt2_cli --features vnt-ipc`：通过。
- `cargo build --release -p vnt2 --bin vnt2_ctrl --features vnt-ipc`：通过。
- `cargo build --release -p vntc_manager`：通过。
- `D:\flutter\bin\dart.bat format lib\core\models.dart lib\core\vnt_runtime.dart lib\core\dashboard_controller.dart lib\main.dart`：通过。
- `D:\flutter\bin\flutter.bat analyze`：通过，`No issues found!`
- `D:\flutter\bin\flutter.bat build windows --release`：
  - 第一次在中文路径下失败，MSBuild 读取 `.dart_tool` 路径时出现乱码。
  - 使用临时 ASCII 盘符 `V:` 映射项目目录后重新构建成功。
- 已执行 `flutter clean` 清理 Flutter 中间缓存。
- `Test-Path V:\` 返回 `False`，临时盘符已移除。

## 构建产物

输出目录：`output/vntc2_flutter-windows-release`

关键文件 SHA256：

```text
vntc2_flutter.exe
9536AC7FACC8A062AC74435D69E04F90B8C2A7952FB4E189E318516A14AC69B4

vntc_manager.exe
80F0830EB63219898C647C34AA0DF3A542DC8053E8D4058D08BDD779C30FBDFE

vnt2_cli.exe
DD161A334C8DEFEE5834FEFB6FC678A7B05A04C5B9BDDCE39AE62CCC6B3CDDFD

vnt2_ctrl.exe
FEF65A5FFE6984A82B0EB695A13F4FC90216A2034813B2BA17E2548FF37B2F58
```

## 待办清单（未完成）

- 如需发布安装包，还需要基于 `output/vntc2_flutter-windows-release` 重新打包安装器或 zip。
- 当前工作区仍有本次开始前就存在的 README 删除和若干未跟踪目录，未纳入本次功能修改。

## 下一步要做什么

本次功能开发、验证、构建产物更新和缓存清理已完成。后续可按需要进行发布打包。
