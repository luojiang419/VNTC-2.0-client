# 015-应用内更新功能接入

## 已完成内容

- 新增 GitHub Release 更新模块：
  - `vntc2_flutter/lib/features/updater/domain/app_update_config.dart`
  - `vntc2_flutter/lib/features/updater/domain/update_models.dart`
  - `vntc2_flutter/lib/features/updater/data/updater_service.dart`
  - `vntc2_flutter/lib/features/updater/application/updater_controller.dart`
- 新增更新包存放目录：
  - `AppPaths.updatesDir` -> `data/updates`
- 在 `AppSettings` 中新增待更新状态字段：
  - `pendingUpdateVersionTag`
  - `pendingUpdateInstallerPath`
  - `dismissedUpdatePromptVersion`
- 在主界面接入：
  - 启动后自动检查 GitHub 最新 Release。
  - 底部新增“检查更新”图标按钮。
  - 托盘菜单新增“检查更新”。
  - 下载完成后弹窗提示“下次启动更新 / 立即更新”。
- 已执行验证：
  - `D:\flutter\bin\flutter.bat pub get`：通过。
  - `D:\flutter\bin\flutter.bat analyze`：通过，`No issues found!`

## 当前修改到哪个模块

模块 3 与模块 4：更新服务、控制器和 UI 接入完成。

## 具体修改的代码前后对比

### 更新模块

修改前：当前项目没有 `features/updater` 模块。

修改后：新增配置、模型、服务和控制器四层结构。

```text
features/updater/domain/app_update_config.dart
features/updater/domain/update_models.dart
features/updater/data/updater_service.dart
features/updater/application/updater_controller.dart
```

### 更新包目录

修改前：

```dart
final Directory logsDir;
```

修改后：

```dart
final Directory logsDir;
final Directory updatesDir;
```

### 设置持久化

修改前：`AppSettings` 未记录待安装更新。

修改后：

```dart
final String? pendingUpdateVersionTag;
final String? pendingUpdateInstallerPath;
final String? dismissedUpdatePromptVersion;
```

### 主界面入口

修改前：底部右侧只有新增配置和设置按钮。

修改后：新增更新按钮，并使用 `UpdaterController` 状态显示下载/待更新图标。

```dart
ValueListenableBuilder<UpdaterState>(
  valueListenable: widget.updaterController,
  builder: (context, updateState, _) {
    return _IconCircleButton(
      icon: updateState.isBusy
          ? Icons.downloading_rounded
          : updateState.hasReadyUpdate
          ? Icons.system_update_alt_rounded
          : Icons.update_rounded,
      onPressed: updateState.isBusy ? null : () => _checkForUpdates(context),
    );
  },
)
```

## 待办清单（未完成）

- 将本地测试版本更新到 `v1.1.4` 并编译低版本测试包。
- 将线上发布版本更新到 `v1.1.5` 并编译 Release 包。
- 更新安装脚本和发布说明。
- 提交、推送分支。
- 创建 GitHub Release 并上传资产。
- 核验 Release 资产与更新链路。

## 下一步要做什么

进入模块 5：生成本地低版本测试包与线上高版本 Release 包。
