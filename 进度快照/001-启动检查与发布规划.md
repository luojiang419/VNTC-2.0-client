# 001-启动检查与发布规划

## 已完成内容

- 已读取项目根目录，确认没有历史进度快照需要续写。
- 已确认客户端工程位于 `vntc2_flutter`。
- 已确认当前版本为 `1.1.1+3`，线上最新 GitHub Release 为 `v1.1.1`。
- 已确认 Windows 应用图标引用位置为 `vntc2_flutter/windows/runner/resources/app_icon.ico`。
- 已确认安装包图标来源为 `dist/app_icon.ico`，安装脚本为 `release-artifacts/vntc_setup.iss`。
- 已创建阶段备份：`backup/001-程序图标更新前备份.md`。

## 当前修改到哪个模块

模块 1：启动检查、发布规划与阶段备份。

## 具体修改的代码前后对比

新增文档：

```text
backup/001-程序图标更新前备份.md
进度快照/001-启动检查与发布规划.md
```

版本文件和图标文件暂未修改。

## 待办清单（未完成）

- 设计并生成简约程序图标。
- 替换 Windows Runner 图标与 `dist/app_icon.ico`。
- 将版本提升到 `1.1.2+4`。
- 更新安装脚本和 Release Notes。
- 编译 Flutter Windows 新版本。
- 生成 zip/安装包发布资产。
- 提交、推送并创建 GitHub 更新入口。

## 下一步要做什么

进入模块 2：生成并应用新的简约程序图标。
