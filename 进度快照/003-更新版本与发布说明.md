# 003-更新版本与发布说明

## 已完成内容

- 将 Flutter 客户端版本从 `1.1.1+3` 更新为 `1.1.2+4`。
- 将 Windows Runner 资源 fallback 版本从 `1.1.1` 更新为 `1.1.2`。
- 将 Inno Setup 安装脚本版本和输出文件名更新为 `1.1.2`。
- 将安装脚本中的固定旧路径改为相对脚本路径：
  - `MySourceDir` 指向 `release-artifacts/../dist`
  - `MyOutDir` 指向 `release-artifacts`
- 更新 `release-artifacts/RELEASE_NOTES.md` 为 v1.1.2 图标更新发布说明。

## 当前修改到哪个模块

模块 3：版本号、安装脚本和发布说明。

## 具体修改的代码前后对比

```diff
-version: 1.1.1+3
+version: 1.1.2+4
```

```diff
-#define VERSION_AS_NUMBER 1,1,1,0
+#define VERSION_AS_NUMBER 1,1,2,0

-#define VERSION_AS_STRING "1.1.1"
+#define VERSION_AS_STRING "1.1.2"
```

```diff
-#define MyAppVersion "1.1.1"
+#define MyAppVersion "1.1.2"

-#define MySourceDir "D:/app/VNT2X/dist"
-#define MyOutDir "D:/app/VNT2X/release-artifacts"
+#define MySourceDir AddBackslash(SourcePath) + "..\\dist"
+#define MyOutDir SourcePath

-OutputBaseFilename=VNTC-2.0-client-v1.1.1-setup
+OutputBaseFilename=VNTC-2.0-client-v1.1.2-setup
```

```diff
-# VNTC 2.0 Client v1.1.1
+# VNTC 2.0 Client v1.1.2
```

## 待办清单（未完成）

- 编译 Flutter Windows 新版本。
- 生成 zip/安装包发布资产。
- 校验新产物包含新图标。
- 提交、推送并创建 GitHub 更新入口。

## 下一步要做什么

进入模块 4：执行 Flutter Windows 构建并生成发布资产。
